export const propsDiff = (oldObj, newObj) => {
  // let difference = false;

  Object.keys(oldObj).forEach((key) => {
    // check for the props in the new obj
    if (newObj[key]) {
      // check for difference between

      if (oldObj[key] === newObj[key]) {
      }
    }
  });
};
